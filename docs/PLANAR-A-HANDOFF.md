# PLANAR-A Handoff

Status: **PLANAR-A RESOLUTION CONTRACT CORRECTION — IMPLEMENTED / AUTOMATED TECHNICAL PASS / SAME-INPUT FIXTURE USER VISUAL PASS**. This correction does not authorize Planar-B or close practical-image end-to-end delivery validation.

`PLANAR-A-RESOLUTION-CONTRACT-CORRECTION.md` is the current resolution/source authority. The earlier 4728 × 5760 *Planar input* assumption is superseded. `PLANAR-MAPPING-TRANSPLANT-AUDIT.md` is a historical audit with a correction addendum; `PLANAR-A-VALIDATION.md` records automated tests, same-input fixture user acceptance, and deferred end-to-end delivery validation.

The existing Family Profile owns each working/composite size: FRONT 75F 3000 × 3840, BACK 2100 × 3840. The Planar source is **current-family BAKE FULL MERGED `DIRECT`**, not selected-layer Direct and not the 4728 × 5760 Full Merge `CANONICAL` technical output. `PLANAR_OUTPUT_PROFILE` separately owns LUUX Planar Master 4728 × 5760. The baker GLBs, exact nodes, UVs, fixed camera, and output frame remain unchanged.

The future Planar-B caller must require a current-family Full Merge ready state and pass its `mergedRevision` as `mergedDirectRevision` to `readPlanarSource(fullMergeRuntime, familyId, { ready, mergedDirectRevision })`. It must pass the returned input to `PlanarMappingRuntime.render(familyId, input, { sourceMergedDirectRevision: mergedDirectRevision })`, release returned CPU bytes, and invalidate the respective family when Full Merge/authoring becomes stale. The adapter verifies the Full Merge result revision and reads only `readOutputRgba(familyId, 'DIRECT')`. Planar state remains independent by family; a legacy Canonical-only change is not its revision authority. This lifecycle is **not** wired to user-facing BAKE PLANAR/SAVE PLANAR PNG in this correction turn.

The real FRONT/BACK JPGs, existing-baker PNGs, difference images, and a real-footage capture are preserved under `2DAsset/Calibration/PlanarBakeTest/`. They support the family-native input contract and practical baker operation. Separately, the user compared the FRONT/BACK **same-input asymmetric fixtures** in `baker.html` and the new Signage runtime, accepted geometry/placement, and reported no visible halos over black or white. Alpha channel bytes and opaque RGB match exactly; partial-alpha RGB differs. This closes only **fixture visual parity**, not practical-image end-to-end delivery. The source GLBs are user-owned untracked files and must be explicitly included in any future release commit. `luux-mockup/` oracle files remain read-only.

Block 8F Full Merge Direct/Canonical, Block 9B-B Canonical Photoshop target, project schema v4, and the UI are unchanged. Planar-B, Planar Photoshop target/SEND, 9B-C, 9C, 90F, ILMIN, and SYNC implementation remain deferred.
