# Block 9B-B Handoff — Photoshop Target Quick Create / Register

**Status: IMPLEMENTED / AUTOMATED TECHNICAL PASS / USER PHOTOSHOP A–I PASS / CLOSEOUT PENDING**  
Date: 2026-09-14  
Branch: `main`  
Base HEAD: `ac3924677c0098e431fcfdbfbff97d4ee1f2043b`

The UXP Target section now puts FRONT75/BACK Direct Quick Create first, current-family Canonical second, and retains the existing manual registration controls. One explicit click requests one blank transparent RGB8/sRGB document at its exact output size, re-reads the actual document by returned ID, validates it, and registers it through the existing session BakeTargetRegistry. READY is reported only after registry resolution confirms that same document. Existing READY prevents duplicate creation; closed/changed targets can be explicitly recreated. Creation, validation, and registration failures remain visible and never silently close the new document.

A small renderer → broker → UXP `TARGET_FAMILY_CONTEXT` status message carries current FRONT75/BACK/null only for Canonical binding. It carries no pixels or job data and does not change the existing Live/Snapshot/Bake binary transport. There is no automatic document scan, name-based identity, startup create, UXP-reload reconnect, target persistence, or new Send action. Direct mismatch safety and manual registration remain. Project schema is still v4. Block 9B-A and POST-9B-A preview-background remain CLOSED and unchanged.

Code: `photoshop-uxp/luux-live-link/target-quick-create.js`, `index.js`, `index.html`, `styles.css`; context relay in `desktop-app/src/renderer.js` and `live-link-broker.cjs`; test `desktop-app/tests/block-9bb-validation.mjs` and package test wiring. [Validation](BLOCK-9B-B-VALIDATION.md) separates automated evidence from real Photoshop user gates. The user reported FRONT75/BACK Quick Create/Register, READY, and actual `SEND DIRECT → APPLIED` as PASS on 2026-09-14, then confirmed C–G. G was corrected to no automatic Bake Target binding. The user also reported that a wrong-size registered canvas blocks `SEND DIRECT` (H PASS). After comparing an existing content PSD with its duplicate, the user additionally ran manual `REGISTER ACTIVE TARGET` → `CLEAR` → `CREATE + REGISTER` and confirmed the old content did not change (I PASS). Earlier mismatched-source screenshots concerned `FROM PHOTOSHOP COMPOSITE` and `FROM PHOTOSHOP SELECTION`, not the Bake Target gate; a separate [Snapshot resolution consent flow](SNAPSHOT-RESOLUTION-CONSENT-VALIDATION.md) now warns before importing from a mismatched source document. Gates A–I are recorded as user PASS. Formal repository closeout remains pending; do not conflate the accepted visual gate with a commit or clean worktree.

Deferred: 9B-C Photoshop input expansion (Pixel Layer + Layer Mask, Smart Object, multi-select flattened Snapshot, Group low priority); 9C Source Link/Changed/Refresh/conflict/Reconcile; future mandatory planar mapping Bake/export/target. Automatic target search/reconnect, SEND FULL MERGED, and 16/32-bit workflows remain unsupported.
