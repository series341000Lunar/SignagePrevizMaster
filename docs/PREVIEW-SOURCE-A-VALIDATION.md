# PREVIEW-SOURCE-A — Validation

Baseline HEAD: `cbef701eb569de73f6f4f36218b93bfbc95c1fc5`.

Implementation gates: PHOTO/SITE source selectors, separate session memories, simple IMAGE preview file and bundled asset controls, existing Photoshop Live frame routing, honest AUTHORING not-ready/outdated/unavailable states, and preservation of source-independent camera/view/project state. AUTHORING workspace has no selector.

Runtime evidence: `desktop-app/.runtime/preview-source-a-runtime.json` reports `PREVIEW_SOURCE_A_TECHNICAL_PASS=true`. PHOTO Front, Front Sweet, Back, Night and SITE Free, Front 75F, Back each switched IMAGE → PS PREVIEW → AUTHORING → IMAGE without changing their camera, location, or site selection. IMAGE and synthetic valid PS frame were mapped to the selected signage meshes; the synthetic frame at anamorphic Direct-resolution mismatch showed `SIZE MISMATCH` and retained the last valid display. Disconnect showed `UNAVAILABLE` and retained the last valid display. PHOTO/SITE source memories remained independent.

A synthetic temporary PNG loaded through the IMAGE file path; Authoring revision and project status remained unchanged. Layer IDs, family, transform, vector mask, opacity/blend, merge/Planar job state and project status stayed unchanged through the source smoke. Synthetic ready Planar result was reported as `UNAVAILABLE · physical display pending` because physical mesh routing does not exist in A; dirty result reported `OUTDATED`, neither triggered Bake, and the last valid image stayed mapped. The existing PHOTO/SITE physical preview cannot claim AUTHORING READY in this block. Real Photoshop host and GUI visual confirmation were not performed by the synthetic smoke and remain a separate practical gate.

Follow-up IMAGE orientation/alpha correction: the Electron source smoke used an 8×8 PNG with opaque red, partial-alpha green, and transparent cutout regions. The decoded source remained RGBA (transparent pixel A 0). On the GPU, the oriented top-left pixel was `(255,0,0,255)`, the bottom-left cutout was `(0,0,0,255)`, and the partial-alpha region rendered over black with A 255. The bundled IMAGE texture had `flipY=false` while its 2D and 3D Plane geometry UVs were inverted to preserve their prior orientation. `PREVIEW_SOURCE_A_TECHNICAL_PASS=true` and the existing general Electron regression reported `BLOCK0_TECHNICAL_PASS=true`. User visual confirmation of a production PNG remains separate.

Follow-up IMAGE bank: the source smoke loaded five distinct named PNG files, reselected a cached file through the dropdown, and verified that the sixth load was rejected without replacing the bank. Clicking trash removed exactly the selected bank entry; the other four textures and options remained. The bundled IMAGE selection disabled trash and a click left the bank unchanged. The smoke then deleted the remaining files one at a time and returned to the bundled IMAGE selection. Authoring revision and project status remained unchanged. `PREVIEW_SOURCE_A_TECHNICAL_PASS=true`; the general Electron regression again reported `BLOCK0_TECHNICAL_PASS=true`. The bank is session-only and does not persist in the project.

Long-name layout follow-up: an Electron smoke loaded a synthetic 73-character PNG name and checked that its dropdown label was shortened to at most 32 characters, its full name remained in the tooltip, and both the dropdown/trash row and filename line stayed within the visible Workspace card. `npm run test:static` and `PREVIEW_SOURCE_A_TECHNICAL_PASS=true` passed after this edit. Real user GUI visual confirmation of this layout remains separate.

Regression evidence: `npm run test:static` and `npm run test:protocol` passed. The existing general Electron smoke passed after routing was limited to the actual Production workspace scene. Phase A PHOTO locations/navigation lock, SITE camera and FOV/reset controls, AUTHORING family reset, and quick Bake menu all passed in the Production UI smoke section. The final general smoke result is recorded at `desktop-app/.runtime/dev-runtime.json`.

Schema change: NO. Bake algorithm change: NO. Planar algorithm change: NO. Photoshop protocol change: NO. Automatic Bake: NO. Planar-to-Photoshop Send: NO.

Closure: `PREVIEW-SOURCE-A — CLOSED / UNIFIED PREVIEW SOURCE FOUNDATION` (technical gate). Real Photoshop/visual user PASS is not implied.
