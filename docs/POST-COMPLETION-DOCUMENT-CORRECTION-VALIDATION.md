# LUUX Signage Previz — Post-completion document correction

Date: 2026-09-14 (KST)  
Baseline: local `main` / `6aa4763b3a2f6c1ab1c36694c37e3030bd9c3e58` (`Add Planar B output workflow and runtime smoke coverage`), tracking `origin/main`. At the start of this correction the only working-tree item was the untracked [Block 9B-C1 probe](BLOCK-9B-C1-PROBE.md). The previous two commits were `c5ecc8f` and `a56bacb`.

## Current contract and preserved evidence

- **Block 9B-C1: CLOSED / UNSUPPORTED / NON-BLOCKING; implementation NONE; further implementation NOT PLANNED.** The [Photoshop 2026 real-host probe](BLOCK-9B-C1-PROBE.md) found that `imaging.getPixels({layerID})` returned opaque pixels for a fully masked internal hole and no partial alpha from 20 px user Layer Mask Feather. The apparent hard-mask success arose from `boundsNoEffects` shrinking; Photoshop Layer Opacity 62% remained separate metadata. A mask-dependent Pixel Layer can currently pass the `LayerKind.NORMAL` code gate but its visible-result Snapshot is **not** in the supported product contract. No Mask Guard is planned.
- **Manual workaround:** preserve the original artwork when needed by duplicating the layer or working document in Photoshop; apply/merge/rasterize the mask result into an ordinary Pixel Layer on that copy; then use `FROM PHOTOSHOP SELECTION`. SMG does not automatically change Photoshop artwork or recreate Mask Density, Feather, interpolation, `getLayerMask()` compositing, or Photoshop Mask → SMG Vector Mask conversion.
- The original [post-8F Block 9 design](POST-BLOCK-8F-SNAPSHOT-AUDIT-AND-BLOCK9-DESIGN-V1.md), [Block 9A validation](BLOCK-9A-VALIDATION.md), [BACK V3 sync roadmap](BACK-V3-ROUNDTRIP-PASS-AND-PHOTOSHOP-SYNC-ROADMAP.md), and [9A](BLOCK-9A-HANDOFF.md)/[9B-A](BLOCK-9B-A-HANDOFF.md)/[9B-B](BLOCK-9B-B-HANDOFF.md) handoff records retain their historical statements and now carry later status notes. [PLANAR-B handoff](PLANAR-B-HANDOFF.md) retains its once-accurate “uncommitted” sentence and adds the subsequently verified committed HEAD. [Desktop README](../desktop-app/README.md) distinguishes a code gate accepting `LayerKind.NORMAL` from supported mask-free Selection Snapshot behavior and identifies historical schema 3 versus current schema **v4**.
- Earlier block gates stay separate: [9A](BLOCK-9A-VALIDATION.md) CLOSED / USER VALIDATED, [9B-A](BLOCK-9B-A-VALIDATION.md) CLOSED / USER PASS, [post-9B-A PREVIEW BACKGROUND](POST-9B-A-PREVIEW-ALPHA-BACKGROUND-VALIDATION.md) CLOSED / USER PASS with schema v4, [9B-B](BLOCK-9B-B-VALIDATION.md) CLOSED / USER PHOTOSHOP PASS, [PLANAR-A correction](PLANAR-A-RESOLUTION-CONTRACT-CORRECTION.md) implemented with scoped fixture visual acceptance, and [PLANAR-B](PLANAR-B-VALIDATION.md) CLOSED with practical FRONT/BACK user PASS. The current Planar source is **current-family Full Merged Direct**, FRONT 75F `3000 × 3840` or BACK `2100 × 3840`, and delivery is a separate `4728 × 5760` RGBA Planar PNG. Existing technical Canonical output is distinct from Planar Master. Earlier design-stage “future Planar” statements are historical, not a present missing core pipeline.

## Enhancement ordering after completion

| Priority | Scope / constraint |
| --- | --- |
| **P0 — NEXT: Production UI Phase A** | Production Shell / Developer UI separation. Preserve the developer-oriented controls; make a practical production-oriented interface the default. Both interfaces must share the **same engine, schema v4, authoring state, Bake pipeline and Photoshop protocol**. Current `BLOCK 8A`, `3D PLANE`, `PROJECTION POC`, `RUN TEST BAKE`, `CANONICAL REPROJECTED`, `OPTIONAL BAKE MASK`, and diagnostics labels are migration candidates for a **later** UI implementation, not edits in this documentation turn. |
| **P1 — practical delivery** | Clarify the already present explicit `BAKE FULL MERGED → BAKE PLANAR → SAVE PLANAR PNG` delivery workflow rather than rebuild it; assess Review Still batch/export, Layer convenience gaps (Duplicate/Lock/Solo, alignment, nudge, Undo/Redo), and project recovery/recent-project/output-history gaps against existing behavior first. |
| **P2 — Photoshop iteration** | Consider explicit Snapshot bitmap replacement of an **existing** SMG Layer, then user-triggered `REFRESH FROM PHOTOSHOP` retaining layer identity, transform, and local Vector Mask by default. `SOURCE CHANGED`/`LINK BROKEN` only after reliable source identity/reconnection is proved. No automatic Refresh or feedback loop. |
| **P3 — demand-driven** | Separate probes for Smart Object Snapshot and multi-select flattened Snapshot; SMG Vector Mask Feather or bitmap alpha map; simple shape/line/arrow, Bezier/freehand, text; Preview Camera Animation/JPG sequence; video texture preview PoC. Each would need its own mini-block and evidence. The Photoshop **user Layer Mask** is not in this list. |

**HOLD / conditional:** ILMIN A-QUBE requires an external C4D production contract; FRONT 90F needs recalibration; SYNC Family is a future family, unrelated to Photoshop synchronization; anamorphic inverse POINT needs proof; `SEND FULL MERGED`, Planar → Photoshop direct send, and advanced partial-transfer/cache optimization need demonstrated production need or measured bottleneck. None is reopened here.

**NO-GO / current scope:** Photoshop user Layer Mask or Group hierarchy recreation inside SMG, automatic Snapshot → Bake → Photoshop feedback, RGB16/32-bit Snapshot and HDR/high-end color pipeline, hidden RGB bit-exact guarantee, full DCC timeline/Graph Editor/Photoshop replacement editor, and Cloud sync. These are scope decisions, not failed tests of the closed production pipeline.

## Correction boundaries and verification

The explicitly named `LUUX_PostCompletion_Document_Correction_and_Enhancement_Roadmap_V1.md` and `LUUX_Production_UI_Master_Design_V1.md` were **not found** in the repository or the checked Desktop/Documents/internal-project folders. Accordingly, P0 designation and priority wording above are taken from the supplied correction directive and verified completed handoffs, **not** from a claimed reading of those two absent design files. The exact Master Design file's contents/conflicts cannot be independently reconciled in this checkout; no replacement design was invented. The historical post-8F Block 9 design present in `docs/` was updated with superseding status notes.

Corrections are documentation/README only. **Product code changed: NO. Schema changed: NO (current v4). Runtime changed: NO.** No test claim is added to earlier automated or user gates. `git diff --check` passed for tracked edits; all 12 changed Markdown files (including two untracked documents) passed a relative-link and trailing-whitespace check; `git status` and `git diff -- docs/ desktop-app/README.md` confirmed a documentation-only set. Full Runtime/Portable tests are unnecessary for text-only changes. The files are left reviewable and uncommitted; the [9B-C1 probe](BLOCK-9B-C1-PROBE.md) remains untracked but is part of this document set. **Next separate block: PRODUCTION UI PHASE A — Production Shell / Developer UI Separation.**
